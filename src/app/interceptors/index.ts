import axios from 'axios';

export function setInterceptor() {
  // axios.interceptors.request.use(config => {
  //   const newConfig = {
  //     ...config,
  //   };
    // newConfig.headers['X-AUTH-TOKEN'] = localStorage.getItem('user')
    //   ? JSON.parse(localStorage.getItem('user')).token
    //   : 'null';
    // return newConfig;
  // });
}
